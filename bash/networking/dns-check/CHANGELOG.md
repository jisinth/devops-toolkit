# Changelog — dns-check

## [1.0.0] - 2026-07-31

### Added
- Initial release: resolves a host across multiple record types via `dig`/`host`/`nslookup` (auto-detected)
- `--types` and `--resolver` options, per-type query timing
