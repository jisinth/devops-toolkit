# Changelog — docker-clean

## [1.0.0] - 2026-07-31

### Added
- Initial release: `--containers`, `--images`, `--volumes`, `--networks`, `--build-cache`, `--all`
- `--dry-run` mode and confirmation prompt before destructive volume removal
- Before/after `docker system df` report
