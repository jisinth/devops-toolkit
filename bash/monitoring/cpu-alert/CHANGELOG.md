# Changelog — cpu-alert

## [1.0.0] - 2026-07-31

### Added
- Initial release: samples CPU usage via `/proc/stat`, alerts (non-zero exit) above `--threshold`
- `--log-file` and best-effort `--webhook` alert support
