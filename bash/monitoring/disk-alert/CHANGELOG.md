# Changelog — disk-alert

## [1.0.0] - 2026-07-31

### Added
- Initial release: checks disk usage via `df` for `--mount` or `--all` mounts, alerts above `--threshold`
- `--log-file` and best-effort `--webhook` alert support
