# Changelog — website-health

## [1.0.0] - 2026-07-31

### Added
- Initial release: checks HTTP status/response time for one or more URLs (positional or `--url-file`), alerts on non-2xx/timeout
- `--log-file` and best-effort `--webhook` alert support
